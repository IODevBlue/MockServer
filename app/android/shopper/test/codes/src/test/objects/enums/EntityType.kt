package com.blueiobase.app.android.shopper.data.enums

/**
 * Enum class representing the type for each entity in the data layer.
 *
 * A use case of this class is during searching and filtering.
 * @author IO DevBlue
 * @since 1.0.0
 */
enum class EntityType {
    ADDRESS, ANONYMOUS_USER, BUYER, COMPANY, PRODUCT, SELLER
}