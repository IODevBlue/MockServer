package com.blueiobase.app.android.shopper.data.enums

/**
 * Enum class representing the type of remote theme in the data layer.
 * @author IO DevBlue
 * @since 1.0.0
 */
enum class ThemeType {
    /** Light remote theme. */
    LIGHT,
    /** Dark remote theme. */
    DARK
}